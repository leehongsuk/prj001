<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="SqlSampleMapper">
     
    <select id="selectSample_ora" parameterType="java.util.HashMap" resultType="java.util.HashMap">

SELECT *
  FROM SCOTT.EMP
 WHERE EMPNO = #{empno}
          
    </select>
     
     
    <select id="selectSample_my" parameterType="java.util.HashMap" resultType="java.util.HashMap">

        SELECT * FROM USER 
        WHERE User = #{User}
        
         
    </select>
     
    <select id="selectSample_" parameterType="java.util.HashMap" resultType="java.util.HashMap">

        select * 
          from world.country
         where code = #{code} 
    </select>
     
</mapper>